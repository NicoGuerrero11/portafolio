import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://nicoguerrero11.github.io',
  // Descomenta la siguiente línea si usas GitHub Pages con un repo que no es username.github.io
  // base: '/nombre-de-tu-repo',
});
